import { Text, View } from 'react-native';
import React, { Component } from 'react';

export class NoiseGenerator extends Component {
  render() {
    return (
      <View>
        <Text>NoiseGenerator</Text>
      </View>
    );
  }
}

export default NoiseGenerator;
